# 暂停点备忘

更新时间：2026-06-06
当前主分支状态：`master`，本地领先 `origin/master`

## 当前已完成

- `Milestone 1`：核心 schema、错误模型、AST/Artifact/tool 契约
- `Milestone 2-3`：session create/append、SQLite 持久化、clarify rule-based 流程
- `Milestone 4`：首版 AST 生成与设计版本持久化
- `Milestone 5`：preview 生成，输出 `preview.html` 与 `section-summary.json`
- `Milestone 6`：自然语言 revise、版本递增、`diffSummary`、`nodeDiffs`、preview 重生成
- `Milestone 7-8`：confirm/export 最小交付闭环、导出 manifest、两组固定 smoke 样例
- `阶段 1`：官方 MCP SDK 接入、stdio server 入口、7 个 MCP tools 注册、server 注册测试
- `阶段 2`：README、暂停点与 TODO 状态收敛
- `阶段 3`：preview/export 共享 `CompiledDocument` 编译核心、HTML 片段渲染、annotation/binding 派生和结构一致性测试
- `阶段 4`：LLM-compatible intent provider 接口、rule-based fallback、URL metadata 降级诊断、第三组固定 smoke 样例和基础 CSS 质量提升
- `阶段 5`：`.npmrc` warning 清理、runtime root 环境变量配置、GitHub Actions CI、发布前检查清单
- `阶段 6`：可配置 OpenAI-compatible LLM intent provider、环境变量切换、HTTP 失败 fallback 测试
- `阶段 7`：受限 URL HTML metadata 抓取、环境变量治理、localhost/private IP/content-type fallback 测试

## 当前可用能力

当前项目已经具备一条可运行的 v0 端到端链路：

1. 创建 session
2. 追加 `text` / `url` 输入
3. clarify 判断信息是否足够
4. generate 生成 `v1` AST
5. 输出本地 preview
6. revise 基于 `baseVersion` 生成新版本
7. confirm 指定设计版本
8. export 已确认版本的最小静态交付包

当前对外暴露的 MCP tools：

- `design.session.create`
- `design.session.append_input`
- `design.intent.clarify`
- `design.design.generate`
- `design.design.revise`
- `design.design.confirm`
- `design.export.package`

当前本地入口：

- 契约和本地 handler 聚合：`src/index.ts`
- MCP server 工厂：`src/mcp/server.ts`
- stdio server 启动入口：`src/server.ts`
- 共享编译核心：`src/services/compiler/`
- intent provider 接口：`src/providers/llm/`
- OpenAI-compatible provider：`src/providers/llm/openai-compatible-intent-provider.ts`
- URL metadata parser / resolver：`src/providers/parser/url-parser.ts`
- 运行时路径配置：`src/lib/runtime/paths.ts`
- CI：`.github/workflows/ci.yml`
- 发布前检查清单：`docs/release-checklist.md`

## 启动与验证

安装依赖：

```bash
npm install
```

构建并启动 stdio server：

```bash
npm run build
node dist/src/server.js
```

常用验证：

```bash
npm test
npm run typecheck
npm run build
```

最近一次阶段 7 验证结果：

- `npm test`：32 个测试文件、64 个测试通过
- `npm run typecheck`：通过
- `npm run build`：通过
- stdio smoke：SDK client 可拉起 `dist/src/server.js` 并列出 7 个 MCP tools

## 当前边界

- 当前只提供本地 stdio MCP Server，尚未提供 HTTP transport。
- 默认 intent provider 仍是 rule-based fallback，真实 LLM provider 需要显式环境变量启用。
- URL 默认只从 metadata 派生弱信号；远端 HTML metadata 抓取需要显式环境变量启用。
- 设计产物以结构可用为主，不追求高保真视觉。
- preview/export 已共享基础编译核心；页面壳层和产物输出仍按模式分离。
- `node:sqlite` 的 `ExperimentalWarning` 当前可接受，不作为本阶段阻塞项。
- 外部 URL parser、HTTP transport、截图和视觉 diff 仍是后续增强项。

## 下一步建议

当前 `TODO.md` 中定义的 7 个阶段已完成。后续建议进入新一轮规划：

1. 扩展共享编译管线以支持截图、视觉 diff 或更多导出格式。
2. 视集成需求补 HTTP transport。
3. 接入外部 URL parser。

## 下次恢复时的建议起手动作

1. 阅读 `TODO.md`、本文件与 `README.md`。
2. 查看 `git status --short --branch`。
3. 确认最近验证结果是否仍可信。
4. 从 `TODO.md` 的当前下一步继续。
